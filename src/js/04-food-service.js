import menu from './menu.json';
import cardsTemplate from '../templates/food-cards.hbs';

const refs = {
  menu: document.querySelector('.js-menu'),
};

// const marcup = cardsTemplate(menu);
// console.log('marcup', marcup);
// refs.menu.insertAdjacentHTML('beforeend', marcup);
